{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiSettings": {
    "BaseUrl": "http://localhost:5071", // Uses localhost:5071 when ran locally.
    "PublicUrl": "http://localhost:5000"
  }
}